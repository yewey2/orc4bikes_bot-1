from dotenv import load_dotenv
load_dotenv()  # take environment variables from .env.
import json
from controller import decimal_to_float, set_user_data
from decimal import Decimal

TEST_USER = # Your id here


data = # Grab output from bot when it logs "aft" to console
set_user_data(TEST_USER), data)
